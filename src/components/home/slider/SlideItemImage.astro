---
const {class: className='', src='', alt=''} = Astro.props;
---

<div class={`${className} w-full max-w-full h-[50vh] lg:h-screen bg-red-600`}>
    <img
            class="w-full h-full object-center object-cover"
            src={src}
            alt={alt}
    />
</div>

<slot />